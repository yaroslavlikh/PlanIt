# 📅 PlanIt Bot — ваш умный личный планировщик в Telegram

> **Забудьте о ручном вводе дат и времени.** Просто напишите или скажите:  
> *«Встреча с командой завтра в 15:00 в офисе»* —  
> и PlanIt Bot сам поймёт, **что**, **когда** и **где**, а затем аккуратно добавит это в ваш список задач.

PlanIt Bot — это не просто задачник, а интеллектуальный помощник, который превращает свободную речь в структурированные события. Хотите хранить всё локально? Пожалуйста. Нужно синхронизировать встречу с Google Calendar или Todoist? Легко — выбирайте, куда отправить каждую задачу: в локальную базу, в облако или сразу в несколько сервисов.

Всё управление — через удобные инлайн-кнопки: никаких команд, только интуитивная навигация. Голосовые сообщения? Поддерживается. Конфиденциальность? Ваши данные остаются под вашим контролем.

**PlanIt Bot — это свобода планировать так, как вам удобно, без компромиссов между простотой и мощью.**

---

## ✨ Возможности

### 🧠 Интеллектуальный парсинг
- **Естественный язык**: пишите или говорите задачи так, как думаете
- **Автоматическое извлечение**: дата, время, место, описание — всё распознаётся автоматически
- **Голосовой ввод**: отправляйте голосовые сообщения — бот распознаёт речь и создаёт задачи

### 🔄 Гибкая маршрутизация
- **Локальное хранение**: задачи остаются только у вас
- **Облачные интеграции**: Google Calendar, Todoist, Yandex Calendar
- **Множественный выбор**: отправляйте одну задачу сразу в несколько сервисов
- **Полный контроль**: решайте для каждой задачи отдельно, куда её сохранить

### 🎯 Удобный интерфейс
- **Инлайн-кнопки**: всё управление через удобные кнопки в чате
- **Без команд**: никаких сложных команд — просто навигация
- **Подтверждение**: просматривайте и редактируйте задачи перед сохранением
- **Управление**: просмотр, редактирование и удаление сохранённых задач

### 🔒 Безопасность и приватность
- **Шифрование токенов**: все токены интеграций надёжно зашифрованы
- **Ваши данные**: вы контролируете, где хранятся ваши задачи
- **Локальная база**: можете работать полностью офлайн

---

## 💬 Примеры использования

### Текстовый ввод
```
Вы: "Купить продукты завтра в 18:00"
Бот: [Распознаёт задачу, показывает черновик]
     [Предлагает выбрать: Локально / Google Calendar / Todoist]
```

### Голосовой ввод
```
Вы: [Голосовое сообщение] "Встреча с клиентом в пятницу в 14:00 в кафе на Невском"
Бот: [Распознаёт речь, извлекает структуру]
     [Показывает: Задача, Дата, Время, Место]
     [Предлагает выбрать цели]
```

### Управление задачами
```
Вы: [Открываете список задач]
Бот: [Показывает все задачи с кнопками]
     [✏️ Изменить] [🗑 Удалить] [📤 Отправить в...]
```

---

## 🚀 Быстрый старт

### Установка

1. Клонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd PlanItbot
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. Создайте файл `.env` и заполните необходимые переменные (см. раздел [Настройка](#настройка))

4. Запустите бота:
   ```bash
   python -m bot.main
   ```

### Настройка

Все настройки хранятся в файле `.env`. Необходимо указать:

- `BOT_TOKEN` — токен Telegram бота (получите у [@BotFather](https://t.me/BotFather))
- `OPENAI_API_KEY` — ключ API OpenAI (для LLM и Whisper)
- `DATABASE_URL` — URL базы данных (по умолчанию `sqlite:///./planitbot.db`)
- `ENCRYPTION_KEY` — ключ для шифрования токенов интеграций
- Ключи для интеграций (Google, Todoist, Yandex) — опционально

---

## 📁 Структура проекта

```
PlanItBot/
│
├── bot/                          # Всё, что связано с Telegram
│   ├── __init__.py
│   ├── main.py                   # Точка входа (запуск бота)
│   ├── dispatcher.py             # Настройка роутеров и middleware
│   ├── handlers/                 # Обработчики событий (все — через callback_query)
│   │   ├── __init__.py
│   │   ├── navigation.py         # Главное меню, переходы между разделами
│   │   ├── task_input.py         # Приём текста/голоса → парсинг → черновик
│   │   ├── task_confirmation.py  # Подтверждение задачи + выбор целей
│   │   ├── task_management.py    # Просмотр, редактирование, удаление задач
│   │   └── integrations_ui.py    # Управление подключениями (подключить/отключить)
│   └── keyboards/                # Генерация всех inline-клавиатур
│       ├── __init__.py
│       ├── menu.py               # Главное меню, подменю
│       ├── task_input.py         # Кнопки "Текстом / Голосом"
│       ├── destinations.py       # Выбор целей (локально, Google, Todoist...)
│       └── task_actions.py       # Кнопки у задач: "✏️ Изменить", "🗑 Удалить"
│
├── core/                         # Бизнес-логика
│   ├── __init__.py
│   ├── natural_language_parser.py # Вызов LLM для извлечения структуры из текста/голоса
│   ├── task_router.py            # Логика маршрутизации задачи в выбранные цели
│   └── draft_manager.py          # Управление временными черновиками (с TTL)
│
├── integrations/                 # Подключения к внешним сервисам
│   ├── __init__.py
│   ├── base.py                   # Абстрактный класс интеграции
│   ├── registry.py               # Реестр доступных интеграций (Google, Todoist...)
│   ├── google_calendar.py        # Реализация для Google Calendar
│   ├── todoist.py                # Реализация для Todoist
│   └── yandex_calendar.py        # Реализация для Yandex Calendar (если API доступен)
│
├── storage/                      # Хранение данных
│   ├── __init__.py
│   ├── models.py                 # Модели: User, Task, IntegrationToken
│   ├── database.py               # Инициализация БД (SQLite по умолчанию)
│   ├── crud.py                   # Операции: создать/найти/обновить/удалить
│   └── encryption.py             # Шифрование токенов интеграций
│
├── speech/                       # Обработка голосовых сообщений
│   ├── __init__.py
│   ├── transcriber.py            # Преобразование голоса в текст (Whisper)
│   └── audio_utils.py            # Конвертация форматов (OGG → WAV и т.п.)
│
├── config/                       # Конфигурация
│   ├── __init__.py
│   └── settings.py               # Загрузка переменных окружения (Pydantic)
│
├── utils/                        # Вспомогательные инструменты
│   ├── __init__.py
│   ├── datetime_helpers.py       # Работа с датами, форматирование
│   └── logger.py                 # Логирование
│
├── assets/                       # Статика (иконки, примеры и т.д.)
│
├── requirements.txt              # Зависимости
├── .env                          # Секреты и настройки
├── .gitignore
└── README.md
```

## Описание модулей

### bot/
Модуль, отвечающий за взаимодействие с Telegram API. Содержит обработчики событий, клавиатуры и настройку диспетчера.

- **main.py** — точка входа для запуска бота
- **dispatcher.py** — настройка роутеров и middleware
- **handlers/** — обработчики всех событий (callback_query)
- **keyboards/** — генерация inline-клавиатур для интерфейса

### core/
Бизнес-логика приложения, не зависящая от Telegram API.

- **natural_language_parser.py** — парсинг естественного языка с помощью LLM
- **task_router.py** — маршрутизация задач в выбранные цели
- **draft_manager.py** — управление временными черновиками с TTL

### integrations/
Модуль для работы с внешними сервисами (Google Calendar, Todoist, Yandex Calendar).

- **base.py** — абстрактный класс для всех интеграций
- **registry.py** — реестр доступных интеграций
- **google_calendar.py**, **todoist.py**, **yandex_calendar.py** — конкретные реализации

### storage/
Работа с базой данных и хранение данных пользователей.

- **models.py** — модели данных (User, Task, IntegrationToken)
- **database.py** — инициализация БД
- **crud.py** — операции CRUD
- **encryption.py** — шифрование токенов интеграций

### speech/
Обработка голосовых сообщений.

- **transcriber.py** — преобразование голоса в текст с помощью Whisper
- **audio_utils.py** — конвертация аудио форматов

### config/
Конфигурация приложения через переменные окружения.

- **settings.py** — загрузка настроек с помощью Pydantic

### utils/
Вспомогательные утилиты.

- **datetime_helpers.py** — работа с датами и форматирование
- **logger.py** — настройка логирования

---

## 🔧 Технические детали

### Архитектура

PlanIt Bot построен на современном стеке:
- **aiogram 3.x** — фреймворк для Telegram ботов
- **SQLAlchemy** — ORM для работы с базой данных
- **OpenAI API** — для парсинга естественного языка и распознавания речи
- **Pydantic** — валидация конфигурации
- **SQLite** — база данных по умолчанию (можно заменить на PostgreSQL)

### Принципы работы

1. **Парсинг**: Пользователь отправляет текст или голос → LLM извлекает структуру (задача, дата, время, место)
2. **Черновик**: Создаётся временный черновик с TTL (время жизни)
3. **Подтверждение**: Пользователь просматривает и подтверждает задачу
4. **Маршрутизация**: Выбор целей (локально, Google Calendar, Todoist и т.д.)
5. **Сохранение**: Задача сохраняется в выбранные сервисы

---

## 📄 Лицензия

MIT License

---

## 🤝 Вклад в проект

Приветствуются любые улучшения! Создавайте issues и pull requests.

---

**Сделано с ❤️ для тех, кто ценит своё время**
